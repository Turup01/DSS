
METRO CARE HOSPITAL DSS — START HERE

You do not need programming experience.

1. Install Python from:
   https://www.python.org/downloads/

2. Extract this ZIP.

3. Open the folder containing:
   app.py
   requirements.txt
   START_HERE.txt

4. Click the File Explorer address bar, type `cmd`, and press Enter.

5. Install the packages:
   python -m pip install -r requirements.txt

6. Start the DSS:
   python -m streamlit run app.py

7. Open the Local URL shown by Streamlit, usually:
   http://localhost:8501

IMPORTANT:
This version removes the percentage format strings that caused the
browser-side `[sprintf] unexpected placeholder` error.

If an older Streamlit window is still open:
- go to the Command Prompt running the old app
- press Ctrl+C
- close that browser tab
- then run the command above from this new folder.

The next development stage can add scenario save/load, sensitivity
analysis, bottleneck explanations, and a presentation-ready dashboard.
